<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Ventas | Electronics Store</title>

  <!-- Importo las fuentes que usa todo el panel -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap"
    rel="stylesheet"
  >

  <style>
    /* Variables de colores del panel */
    :root {
      --bg-gradient: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
      --accent: #b388ff;
      --accent-strong: #7b2ff7;
      --accent-strong-2: #f107a3;
      --danger: #ff3b3b;
      --danger-2: #ff0077;
      --text-main: #f1f1f1;
    }

    /* Reset + fuente global */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    /* Fondo principal del panel */
    body {
      background: var(--bg-gradient);
      color: var(--text-main);
      min-height: 100vh;
      display: flex;
    }

    /* ======================
       SIDEBAR IZQUIERDA
       ====================== */
    .sidebar {
      width: 260px;
      background: rgba(10, 10, 25, 0.98);
      border-right: 1px solid rgba(255, 255, 255, 0.08);
      box-shadow: 4px 0 20px rgba(0, 0, 0, 0.4);
      padding: 1.5rem 1rem;
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      position: sticky;
      top: 0;
      height: 100vh;
    }

    /* Logo del panel */
    .sidebar-logo {
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--accent);
      text-shadow: 0 0 15px var(--accent);
    }

    /* Texto pequeño debajo del logo */
    .sidebar-subtitle {
      font-size: 0.85rem;
      opacity: 0.75;
    }

    /* Menú de navegación */
    .sidebar-nav {
      display: flex;
      flex-direction: column;
      gap: 0.4rem;
      margin-top: 1rem;
    }

    /* Links del menú */
    .sidebar-link {
      display: block;
      padding: 0.7rem 0.9rem;
      border-radius: 10px;
      text-decoration: none;
      color: #e0dcff;
      font-size: 0.93rem;
      border: 1px solid transparent;
      transition: 0.25s;
    }

    /* Hover de los links */
    .sidebar-link:hover {
      background: rgba(179, 136, 255, 0.08);
      border-color: rgba(179, 136, 255, 0.5);
      box-shadow: 0 0 12px rgba(179, 136, 255, 0.4);
      transform: translateX(3px);
    }

    /* Link activo */
    .sidebar-link.active {
      background: linear-gradient(135deg, var(--accent-strong), var(--accent-strong-2));
      box-shadow: 0 0 15px rgba(179, 136, 255, 0.7);
      color: #fff;
    }

    /* Footer del sidebar */
    .sidebar-footer {
      margin-top: auto;
      display: flex;
      flex-direction: column;
      gap: 0.6rem;
    }

    /* Botón cerrar sesión */
    .btn-logout {
      background: linear-gradient(90deg, var(--danger), var(--danger-2));
      border-radius: 10px;
      padding: 0.7rem 0.9rem;
      border: none;
      color: #fff;
      text-align: center;
      cursor: pointer;
      box-shadow: 0 0 15px rgba(255, 0, 77, 0.5);
      transition: 0.25s;
    }

    .btn-logout:hover {
      transform: translateY(-2px);
      box-shadow: 0 0 25px rgba(255, 0, 77, 0.9);
    }

    /* Botón volver a la tienda */
    .btn-store {
      padding: 0.6rem 0.9rem;
      border-radius: 10px;
      border: 1px solid rgba(179, 136, 255, 0.6);
      background: rgba(179, 136, 255, 0.12);
      font-size: 0.9rem;
      text-align: center;
      text-decoration: none;
      color: #fff;
      box-shadow: 0 0 10px rgba(179, 136, 255, 0.4);
      transition: 0.25s;
    }

    .btn-store:hover {
      background: rgba(179, 136, 255, 0.25);
      transform: translateY(-1px);
    }

    /* ======================
       SECCIÓN PRINCIPAL
       ====================== */
    .main {
      flex: 1;
      padding: 2rem 3rem;
    }

    /* Encabezado del listado */
    .main-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1.6rem;
    }

    .main-header-title {
      font-size: 1.7rem;
      text-shadow: 0 0 12px var(--accent);
    }

    .chip {
      padding: 0.35rem 0.8rem;
      border-radius: 999px;
      border: 1px solid rgba(255, 255, 255, 0.15);
      background: rgba(0, 0, 0, 0.25);
      font-size: 0.8rem;
    }

    /* Contenedor de la tabla */
    .table-wrapper {
      background: rgba(0, 0, 0, 0.25);
      border-radius: 15px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.5);
      overflow-x: auto;
    }

    /* Tabla */
    table {
      width: 100%;
      border-collapse: collapse;
      min-width: 650px;
    }

    thead {
      background: rgba(0, 0, 0, 0.4);
    }

    th, td {
      padding: 0.8rem 0.9rem;
      text-align: left;
      font-size: 0.9rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.06);
    }

    th {
      color: #e5e0ff;
      font-weight: 500;
    }

    tbody tr:hover {
      background: rgba(179, 136, 255, 0.08);
    }

    /* Botón "Ver detalle" */
    .btn-secondary {
      padding: 0.45rem 0.95rem;
      border-radius: 999px;
      border: 1px solid rgba(179, 136, 255, 0.6);
      background: transparent;
      color: #fff;
      font-size: 0.85rem;
      text-decoration: none;
      cursor: pointer;
      transition: 0.25s;
    }

    .btn-secondary:hover {
      background: rgba(179, 136, 255, 0.25);
      transform: translateY(-1px);
    }

  </style>
</head>
<body>

  <!-- SIDEBAR -->
  <aside class="sidebar">
    <div>
      <!-- Logo -->
      <div class="sidebar-logo">Electronics Store</div>
      <div class="sidebar-subtitle">Panel de administración</div>

      <!-- Navegación -->
      <nav class="sidebar-nav">
        <a href="/admin/dashboard" class="sidebar-link">📊 Dashboard</a>
        <a href="/admin/productos" class="sidebar-link">📦 Productos</a>

        <!-- Sección actual -->
        <a href="/admin/ventas" class="sidebar-link active">🧾 Ventas</a>
      </nav>
    </div>

    <!-- Footer del menú -->
    <div class="sidebar-footer">
      <a
        class="btn-store"
        href="http://127.0.0.1:5500/Electronics_Store/Frontend/index.html"
      >
        🛒 Volver a la tienda
      </a>

      <a class="btn-logout" href="/admin/logout">Cerrar sesión</a>
    </div>
  </aside>

  <!-- CUERPO PRINCIPAL -->
  <main class="main">

    <!-- Encabezado de la sección -->
    <header class="main-header">
      <div>
        <h1 class="main-header-title">Ventas realizadas</h1>
        <p>Revisá el historial de ventas y accedé al detalle de cada una.</p>
      </div>
      <div class="chip">Historial</div>
    </header>

    <!-- TABLA DE VENTAS -->
    <section class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>ID Venta</th>
            <th>Fecha</th>
            <th>Total</th>
            <th>Items</th>
            <th>Acción</th>
          </tr>
        </thead>

        <tbody>
          <% ventas.forEach(v => { %>
          <tr>
            <!-- ID -->
            <td><%= v.id %></td>

            <!-- Fecha formateada (si viene como Date) -->
            <td>
              <% if (v.fecha && v.fecha.toLocaleString) { %>
                <%= v.fecha.toLocaleString() %>
              <% } else { %>
                <%= v.fecha %>
              <% } %>
            </td>

            <!-- Total -->
            <td>$<%= v.total %></td>

            <!-- Cantidad total de items de esa venta -->
            <td><%= v.cantidad_items %></td>

            <!-- Botón para entrar al detalle -->
            <td>
              <a href="/admin/ventas/<%= v.id %>" class="btn-secondary">
                Ver detalle
              </a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </section>
  </main>

</body>
</html>
